<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- IMPORTANTE: NAO usar <login-config><auth-method>OIDC</auth-method>.
         Estamos configurando OIDC manualmente via subsistema Elytron.
         O listener abaixo eh o que liga o app ao mecanismo OIDC do Elytron. -->
    <listener>
        <listener-class>org.wildfly.security.http.oidc.OidcConfigurationServletListener</listener-class>
    </listener>

    <security-constraint>
        <web-resource-collection>
            <web-resource-name>secured</web-resource-name>
            <url-pattern>/secured</url-pattern>
            <url-pattern>/logout</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>*</role-name>
        </auth-constraint>
    </security-constraint>

    <security-role>
        <role-name>admin</role-name>
    </security-role>
    <security-role>
        <role-name>user</role-name>
    </security-role>

    <!-- Caminho onde o listener procura a config OIDC (oidc.json).
         Empacotamos esse arquivo em WEB-INF/oidc.json. -->
    <context-param>
        <param-name>oidc.config.file</param-name>
        <param-value>WEB-INF/oidc.json</param-value>
    </context-param>
</web-app>
